# Deployment Configuration for Render/Railway

# For Render.com deployment
web: uvicorn server:app --host 0.0.0.0 --port $PORT

# Alternative for Railway/Heroku
# web: python -m uvicorn server:app --host 0.0.0.0 --port $PORT